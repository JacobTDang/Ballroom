# Sum of Two Integers

Given two integers `a` and `b`, return the sum of the two integers
without using the `+` and `-` operators.

## Example

```
Input: a = 1, b = 1
Output: 2
```

## Constraints

- `-1000 <= a, b <= 1000`
