# Number of 1 Bits

Given an unsigned 32-bit integer `n`, return the number of `1` bits in
its binary representation (also known as the Hamming weight).

## Example

```
Input: n = 11 (binary: 00000000000000000000000000001011)
Output: 3
```

## Constraints

- `0 <= n <= 2^32 - 1`
