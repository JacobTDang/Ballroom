#!/usr/bin/env bash
set -euo pipefail

# Launches the 3-pane tmux session: editor (nvim) / terminal (shell) / tutor
# (chat CLI, see tutor/chat.sh). See interview_prep_mvp_spec.md Section 3.1.
#
# TODO(M0): confirm host.docker.internal:11434 reachability before wiring
# the tutor pane — see spec Section 6, top de-risk item.

echo "entrypoint: not yet implemented" >&2
exit 1
