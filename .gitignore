# Go
/practice
/verify-exercises
/tutor-eval
*.test
*.out

# SQLite tracker (local practice data, not shared)
data/*.db
data/*.db-journal

# Persisted user settings (e.g. picked tutor model), not shared
data/settings.json

# OS
.DS_Store

# Whisper model for ballroom voice (downloaded on first use, ~142 MB)
data/whisper/
data/transcripts/
